"""detectors — Reflexarc Tier-1 pattern detectors.

Each detector extends BaseDetector (base.py) and implements detect().
The Kyoko prevalence + recurrence layer is baked into BaseDetector so
all detectors inherit it for free.
"""
