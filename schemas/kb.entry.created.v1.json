{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "kb.entry.created.v1",
  "title": "KB Entry Created",
  "description": "SUPERSEDES the prior 'Currently silent — defines what the event SHOULD contain once kb ingest is updated to emit it' claim (2026-08-23 emission re-audit, kb-recall-loop): this channel is LIVE, not silent — source-verified via grep of publish_to_bus(\"kb.entry.created.v1\", ...) call sites (src/commands/ingest.rs:390, src/commands/landmark.rs:136, src/commands/watch.rs:658 and :1321) and confirmed against the live nbus:kb.entry.created.v1 Redis stream (XLEN > 10000 at time of audit) and recent debug.jsonl history. Emitted by kb ingest, kb landmark, and two kb watch handlers, each carrying the required project field as a plain string. Primary consumers: hearth-api (counter), deer-flow (awareness of new knowledge) — NOT independently re-verified in this pass; only the emission side was audited here. See nervous-bus schemas/CHANNELS.md for current status.",
  "type": "object",
  "required": ["entry_id", "title", "project", "source_type", "confidence"],
  "properties": {
    "entry_id":    { "type": "string", "format": "uuid" },
    "title":       { "type": "string", "maxLength": 200 },
    "project":     { "type": "string" },
    "category":    { "type": "string", "enum": ["research", "design", "handoff", "plan", "reference", "log"] },
    "source_type": { "type": "string", "enum": ["deer-flow-research", "deer-flow-guidance", "tengine-session", "test-result", "manual", "agent-generated", "bead-outcome"] },
    "confidence":  { "type": "number", "minimum": 0, "maximum": 1 },
    "trace_uri":   { "type": "string", "description": "deer-flow://<thread>, bd://<bead>, tengine-session://<dir>" },
    "tags":        { "type": "array", "items": { "type": "string" }, "maxItems": 20 },
    "file":        { "type": "string", "description": "Absolute path to the vault markdown file" }
  }
}
