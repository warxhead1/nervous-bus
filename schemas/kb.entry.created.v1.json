{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "kb.entry.created.v1",
  "title": "KB Entry Created",
  "description": "DEPRECATED legacy compatibility contract for KB entry-created events. The historical v1 producer paths remain documented here for existing records and consumers, but new KB entry-created producer and consumer work uses kb.entry.created.v2. Runtime migration evidence recorded 2026-08-24: v2 activation probe event 01M0RZ2V7GWWHGHCE03Y8YY4RJ did not append to nbus:kb.entry.created.v1, so this migration does not claim a semantic dual-write. Keep this schema until legacy consumers and retained v1 history no longer require it.",
  "deprecated": true,
  "type": "object",
  "required": ["entry_id", "title", "project", "source_type", "confidence"],
  "properties": {
    "entry_id":    { "type": "string", "format": "uuid" },
    "title":       { "type": "string", "maxLength": 200 },
    "project":     { "type": "string" },
    "category":    { "type": "string", "enum": ["research", "design", "handoff", "plan", "reference", "log"] },
    "source_type": { "type": "string", "enum": ["deer-flow-research", "deer-flow-guidance", "tengine-session", "test-result", "manual", "agent-generated", "bead-outcome"] },
    "confidence":  { "type": "number", "minimum": 0, "maximum": 1 },
    "trace_uri":   { "type": "string", "description": "deer-flow://<thread>, bd://<bead>, tengine-session://<dir>" },
    "tags":        { "type": "array", "items": { "type": "string" }, "maxItems": 20 },
    "file":        { "type": "string", "description": "Absolute path to the vault markdown file" }
  }
}
