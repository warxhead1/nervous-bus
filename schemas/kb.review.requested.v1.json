{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "kb.review.requested.v1",
  "title": "KB Review Requested",
  "description": "Emitted by `kb propose <id>` when a vault entry is submitted for human review. Primary consumer: hearth (Android notification), deer-flow (awareness queue).",
  "type": "object",
  "required": ["entry_id", "title", "project", "proposed_by"],
  "properties": {
    "entry_id":    { "type": "string", "format": "uuid" },
    "title":       { "type": "string", "maxLength": 200 },
    "project":     { "type": "string" },
    "proposed_by": { "type": "string", "description": "Identity of the proposer (agent session ID, username, or tool name)" },
    "reviewer":    { "type": "string", "description": "Optional intended reviewer; omit for any-reviewer" },
    "category":    { "type": "string", "enum": ["research", "design", "handoff", "plan", "reference", "log"] }
  }
}
