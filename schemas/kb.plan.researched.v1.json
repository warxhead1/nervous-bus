{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "kb.plan.researched.v1",
  "title": "KB Plan Researched",
  "description": "Emitted by `kb plan-research <id>` when deer-flow research tracks are dispatched for a plan. Enables hearth-loom to correlate bead dispatch with the originating plan.",
  "type": "object",
  "required": ["plan_id", "track_ids"],
  "properties": {
    "plan_id":   { "type": "string", "format": "uuid" },
    "title":     { "type": "string", "maxLength": 200 },
    "project":   { "type": "string" },
    "track_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "deer-flow thread/track IDs dispatched for this plan"
    },
    "live":      { "type": "boolean", "description": "Whether research was dispatched live (true) or dry-run (false)" }
  }
}
