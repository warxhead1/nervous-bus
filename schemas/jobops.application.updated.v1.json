{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "nervous-bus/schemas/jobops.application.updated.v1.json",
  "title": "jobops.application.updated v1",
  "description": "Emitted by job-ops (internal/bus/publish.go, EventApplicationUpdated) when an application record's status changes. Backfilled schema — channel had live Redis traffic with no registered schema.",
  "type": "object",
  "required": ["id", "company_id", "role_title", "status"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Application identifier."
    },
    "company_id": {
      "type": "string",
      "description": "Identifier of the company the application is for."
    },
    "role_title": {
      "type": "string",
      "description": "Title of the role applied to."
    },
    "status": {
      "type": "string",
      "description": "New application status (e.g. 'Evaluated', 'Applied', 'Responded', 'Interview', 'Offer', 'Rejected', 'Discarded', 'SKIP')."
    }
  }
}
